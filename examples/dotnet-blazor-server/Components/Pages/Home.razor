@page "/"
@inject IConfiguration Configuration

<PageTitle>Home</PageTitle>

<h1>Blazor Server + Varlock</h1>

<p>This page keeps the proof narrow: the server host loads Varlock during startup, then a component reads the resulting values from <code>IConfiguration</code>.</p>

<h2>Component Values</h2>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Key</th>
            <th>Value</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var key in VarlockKeys)
        {
            <tr>
                <td>@key</td>
                <td><code>@(Configuration[key] ?? "(null)")</code></td>
            </tr>
        }
    </tbody>
</table>

@code {
    private static readonly string[] VarlockKeys = ["APP_NAME", "COMPONENT_MESSAGE"];
}
