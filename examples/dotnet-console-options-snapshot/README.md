# Options Snapshot Console Example

This example shows `IOptionsSnapshot<T>` request-scoped semantics with Varlock configuration. A scope is created to resolve `IOptionsSnapshot<AppOptions>`, showing that scoped snapshot access works with the Varlock configuration provider.

Run it from this directory:

```bash
dotnet run
```
