# Composition Console Example

This example proves value composition through `${...}` references in the schema. The checked-in `.env` only sets the base inputs; Varlock resolves the composed URLs before they reach `IConfiguration`.

Run it from this directory:

```bash
dotnet run
```