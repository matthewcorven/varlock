# Blazor WASM public configuration (no secrets in client bundle)
# @defaultSensitive=false
# @generateTypes(lang="cs", path="obj/Varlock/VarlockPublicConfig.g.cs", publicOnly=true)
# ---

APP_NAME=MyBlazorWasmApp

# @coerce=number
APP_PORT=5000

# @coerce=boolean
FEATURE_ENABLED=true

# @sensitive
API_KEY=
