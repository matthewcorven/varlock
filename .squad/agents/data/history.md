# Data — History

## Core Context

- **Project:** A first-class Varlock .NET support initiative built around a v1 CLI bridge, proof artifacts and support-matrix validation, and an explicit path to future native runtime or analyzer evolution.
- **Role:** Runtime Lead
- **Joined:** 2026-03-13T10:56:25.544Z

## Learnings

<!-- Append learnings below -->
- **2026-03-17: DX-B1 WebApplicationBuilder.AddVarlock() extension.** Added `VarlockWebApplicationBuilderExtensions` to `Varlock.Extensions.Hosting` behind `#if NET10_0_OR_GREATER`. Mirroring the existing `HostApplicationBuilder` pattern with two overloads that delegate to `builder.Configuration.AddVarlock(...)`. Required multi-targeting the csproj to `netstandard2.0;net10.0` and adding a conditional `FrameworkReference` for `Microsoft.AspNetCore.App`. Key learning: when overriding `TargetFramework` (singular, from Directory.Build.props) with `TargetFrameworks` (plural), must explicitly clear the singular property (`<TargetFramework />`), otherwise MSBuild ProjectReferences resolve to the wrong TFM. Added 3 tests including a reflection-based API shape test. Test project also needed `<FrameworkReference Include="Microsoft.AspNetCore.App" />`.
- **2026-03-17: DX-B3 Static Env.Load() façade.** Created `Varlock.DotNet.Env` as a static class with `Load()` (3 overloads) and `LoadAsync()` (2 overloads). Each call creates a fresh `VarlockCliRuntime()` — no global caching or singleton registration. Added 6 tests: 3 reflection-based API shape tests + 3 integration tests using the `examples/dotnet-console` example. All 9 new tests pass. Created three publishable guides for `.NET` integration under `packages/varlock-website/src/content/docs/integrations/dotnet/`: (1) `getting-started.mdx` covers installation prerequisites, executable acquisition strategies (repo-local, package-local, binary, PATH), package installation, schema setup, and basic usage patterns for ASP.NET and Worker Services. Made explicit that Node.js 22+ is required for the CLI bridge. (2) `configuration.mdx` documents provider precedence order, startup validation, reload-on-change semantics including 300ms debounce and last-known-good preservation on failed reload, User Secrets/appsettings/local.settings.json coexistence, and troubleshooting scenarios. Clarified that configuration errors fail fast at startup by design. (3) `typed-options.mdx` covers C# code generation via `@generateTypes`, binding with `IOptions<T>`, `IOptionsSnapshot<T>`, and `IOptionsMonitor<T>`, validation patterns, Serilog redaction, and public-only generation for Blazor WASM. All guides are faithful to P4-A1 proposal and proof artifacts; avoided shared nav/index edits per constraints. Executable acquisition expectations explicit throughout (lookup order, fallbacks, Windows bridge behavior). Guides anchor to existing proof applications (console, ASP.NET MVC, Worker, Functions, Blazor variants) without overstating support boundaries.
- **2026-03-16: P3-A1c Proof Payload Repair Complete.** Fixed the Functions and Blazor Server `--dump-config` proof paths to reflect actual Varlock-backed configuration instead of emitting defaults. Root cause: both examples attempted `IOptions<T>` binding with root keys like `APP_NAME`, which doesn't map to POCO properties like `AppName`. Applied the established ASP.NET pattern: added MSBuild generation (imports + `VarlockEnabled`, `VarlockSchemaPath`, `VarlockGeneratedFile`), added `@generateTypes` directives to schemas with correct namespace/typeName, and updated snapshot classes to derive directly from `IConfiguration` using generated metadata's `PropertyBindings`, mirroring `AppConfigSnapshot.From(IConfiguration)`. Removed `IOptions` binding and `VarlockFunctionsOptions`/`VarlockBlazorOptions` dependency from `Program.cs`. Post-repair validation: Functions outputs `{"AppName":"varlock-functions","AppPort":7071,"FeatureEnabled":true,"FunctionsOnlyKey":null}`, Blazor Server outputs `{"AppName":"varlock-blazor-server","AppPort":5280,"FeatureEnabled":true}`. Both now prove configuration acquisition at startup without magic binding claims. Generated metadata files in `obj/Varlock/` confirm type generation working as expected.
- **2026-03-16: P3-A1c Framework Examples Complete.** Built Azure Functions isolated and Blazor Server examples proving existing configuration provider and IOptionsSnapshot patterns. Azure Functions uses ConfigureAppConfiguration + AddVarlock; Blazor Server uses IConfigurationBuilder.AddVarlock directly. Both demonstrate honest local.settings.json/appsettings.json coexistence: Varlock is added to the provider chain, so Varlock keys override existing sources by provider order while keys unique to platform-specific config remain available. Functions example documents this explicitly because local.settings.json is a Functions-specific surface. Blazor Server proves server-side config access through IOptionsSnapshot<T> and explicit comments clarifying that configuration never reaches client JavaScript. Both examples follow hosted-proof pattern: --dump-config flag emits machine-readable JSON snapshot for validation. Blazor WASM remains blocked pending publicOnly generation contract from Geordi (not built in this pass). Examples compile; proof:dotnet validation deferred to O'Brien.
- **2026-03-15: Windows executable resolution fixed in P3-A1a.** Picard rejected the initial proof/runtime work because the runtime's lookup methods were platform-agnostic but hardcoded `.js` filenames, so they never found the `.cmd` wrappers the proof harness created on Windows. Fixed by adding `FindExecutableInBinDirectory()` method that prefers `.cmd` on Windows before falling back to `.js` on all platforms. Also updated `FindExecutableInDirectory()` to prefer `.cmd` for `.bin` lookups. Added Windows-platform-specific test to lock down the behavior. No changes to proof harness (already created correct files). All 18 tests pass, proof harness passes. Ready for Windows CI validation.
- 2026-03-16: The `.NET` bridge must preserve lookup order but change launch hosting on Windows when the resolved Varlock executable ends in `.js`; with `UseShellExecute=false`, repo-local and package-local JS entrypoints need `node "<cli.js>" ...` rather than direct execution, and a proof test should use a path containing spaces so quoting regressions fail loudly.
- **2026-03-15: P3-A1 Inventory Complete.** The wider-platform proof node is unblocked with a clear gap analysis: 2 of 7 required example platforms proven (console, ASP.NET MVC), 5 pending (Worker, Functions, Blazor Server, Blazor WASM, WinForms). The bridge runtime is solid; all new work is examples and proof tests. Key dependencies: Serilog scope decision (likely defer to Phase 4), Blazor WASM public-config generation design (Geordi ownership), and platform priority callout (Matthew decision). Recommend phased execution: Worker + Functions (Phase A), Blazor (Phase B pending WASM design), WinForms (Phase C), ledger closure (Phase D). No new runtime packages required unless Serilog or unexpected Hosting patterns emerge from examples.
- The first `.NET` bridge slice should stop at startup-only `IVarlockRuntime` plus `IConfigurationProvider` integration; pulling in hosting helpers or reload semantics before the bridge contract is stable mixes too many failure modes.
- 2026-03-13: Picard fixed the slice boundary so the bridge skeleton should not start before Tuvok's contract fixtures and O'Brien's executable-acquisition specimen are concrete, and the first delivery must stay honest to the proof scope those artifacts support.
- 2026-03-13: The startup configuration provider should flatten structured Varlock values into ordinary `IConfiguration` child paths and avoid synthetic JSON shadow entries, so binder behavior stays predictable while the CLI contract hardens.
- 2026-03-13: Tuvok fixed the first bridge handshake at `varlock load --format json-full --bridge-contract 1` with JSON stdout envelopes for both paths, so the runtime can key off explicit failure categories while keeping `executable-not-found` on the caller side.
- 2026-03-13: The .NET runtime must parse the bridge envelope even on nonzero CLI exits, because contract failures now arrive as stdout JSON plus exit status rather than as stderr-only process failures.
- 2026-03-13: Keeping a tiny internal raw-graph parsing fallback behind the runtime seam lets local validation cover the contract migration without broadening the public API or delaying the bridge handshake switch.
- 2026-03-13: The canonical ledger now fixes the post-launch runtime contract on the stdout bridge envelope and narrows the raw `json-full` fallback to cases where no envelope markers are present, so future bridge work should not reintroduce mixed success-path parsing.
- 2026-03-13: The smallest honest P1-A1 step is to separate launch-time acquisition from post-launch contract parsing by doing a preflight mismatch probe against the existing `load --bridge-contract` surface instead of inventing a new CLI command.
- 2026-03-13: Runtime lookup hardening can improve beyond the explicit repo-relative proof by preferring package-local install layouts and repo-local development layouts before optional `PATH` discovery, which keeps deterministic local behavior without broadening into packaging automation.
- 2026-03-13: O'Brien fixed the proof ledger so P1-A1 can advance with deterministic lookup and preflight handshake hardening while docs and support rows stay pinned to the explicit `ExecutablePath` proof until checked-in examples or CI cover fallback lookup.
- 2026-03-13: The checked-in examples can now honestly omit `ExecutablePath` and prove the runtime's repo-local development lookup from `examples/*` up to `packages/varlock/bin/cli.js`, but that still does not prove package-local, `node_modules/.bin`, or opt-in `PATH` acquisition.
- 2026-03-13: The squad ledger now records that the checked-in proof path really does run without `ExecutablePath` and proves only the repo-local development layout, so broader acquisition fallbacks stay implementation-only until separately exercised.
- 2026-03-13: The smallest honest next P1-A1 proof step after repo-local lookup is a proof-only package-local wrapper under `examples/dotnet-console-net8/node_modules/varlock/bin/cli.js` that delegates to the real CLI and leaves a marker, because that proves actual acquisition precedence without distorting the checked-in example or widening support claims beyond `node_modules/.bin` and opt-in `PATH`.
- 2026-03-13: The package-local proof harness is now a squad decision, so the honest proven no-`ExecutablePath` surface for P1-A1 is repo-local plus package-local lookup only; `node_modules/.bin` and opt-in `PATH` remain unproven until separate artifacts exercise them.
- 2026-03-13: The next honest P1-A1 increment was to prove `node_modules/.bin/varlock` with the same marker-wrapper pattern used for package-local lookup, which let the docs and ledger advance one lookup branch without silently promoting opt-in PATH fallback.
- 2026-03-13: The checked-in proof harness and successful `bun run proof:dotnet` validation now prove repo-local, package-local, and local `node_modules/.bin/varlock` acquisition with `ExecutablePath` omitted, leaving opt-in `PATH` fallback as the only still-unproven P1-A1 lookup branch.
- 2026-03-13: The smallest honest PATH proof is an env-guarded console-example seam that disables local lookup only for `bun run proof:dotnet`, letting a temporary PATH wrapper prove the opt-in branch without changing default example behavior or silently broadening runtime semantics.
- 2026-03-13: After the env-guarded PATH seam landed and fresh `dotnet test ... --filter ResolveExecutable` plus `bun run proof:dotnet` validation passed, the stale proof-planning inbox note was superseded by the checked-in repo state and the canonical P1-A1 lookup surface is now fully proven through repo-local, package-local, local `.bin`, and opt-in PATH acquisition.
- 2026-03-13: The smallest honest P1-A2 expansion on the current startup/runtime slice is User Secrets coexistence on the existing ASP.NET MVC example, because WebApplicationBuilder already loads User Secrets in Development and the proof only needs to show User Secrets-only keys survive while `AddVarlock(...)` still overrides overlapping keys by provider order.
- 2026-03-16: Hosted proof examples stay honest when the app proves itself: keep `Varlock.Extensions.Hosting` as pure `HostApplicationBuilder` delegation, let Worker proof modes self-stop from inside a `BackgroundService`, and prove `IOptionsSnapshot<T>` with plain DI scopes instead of inventing transport-specific semantics.

## P3-A1a Runtime Fixes (2026-03-16)

## Session 2026-03-17T21:30:00Z — DX-B1, DX-B3 Completion

- DX-B1 and DX-B3 implementations completed and all 9 tests passing.
- O'Brien sync'd ledger, READMEs, and control-set board (DX-X1): DX-B1 now green, DX-B3 green (library complete, specimen pending).
- Decisions merged: env-facade pure delegation, hosting multi-target shape.

## P3-A1a Runtime Fixes (2026-03-16)

1. **Windows JS Entrypoint Hosting Decision:** When resolved executable path ends in `.js` on Windows, prefix with `node` in `VarlockCliRuntime.RunProcess`. Rationale: preserves executable-resolution honesty; matches MSBuild bridge pattern.
   - Verification: `dotnet test` + `bun run proof:dotnet` both pass locally
   - Regression test added for `.js` path with spaces (Windows quoting)

2. **Proof Harness `.js` vs `.cmd` Fix (Issue 2):** Package-local harness must create Node.js-executable wrappers (`.js`) on all platforms, not batch files (`.cmd`) on Windows. Runtime's `FindNodeModulesPackageExecutable` is hard-coded to search for `.js`.
   - Assigned from O'Brien via Picard reviewer lockout
   - Status: Fix in progress

## P3-A1 Inventory & Phasing (2026-03-15)

Provided implementation guidance for P3-A1:
- Mapped 5 unproven app-type examples (Worker, Functions, Blazor variants, WinForms)
- Recommended four-phase execution: Phase A (Worker/Functions foundation), Phase B (Blazor, requires design), Phase C (WinForms legacy), Phase D (closure)
- Identified 4 critical decisions needed before P3-A1b starts (platform priority, Serilog scope, snapshot proof, CI matrix timing)

**Status:** P3-A1a fixes pending; P3-A1 phasing documented and ready for Matthew's decisions.
- **2026-03-16: P3-A1c WASM Example Repair Complete.** Fixed the Blazor WASM public-only example to be buildable and honest. The `.env.schema` was already correct with `publicOnly=true`, `typeName=VarlockPublicConfig`, and `path=Generated/VarlockPublicConfig.g.cs`. Primary repairs: (1) converted `App.razor` from HTML document to Blazor component, (2) escaped `@sensitive` as `@@sensitive` in Home.razor markup to avoid Razor compilation errors, (3) added `DotnetBlazorWasmNet8Public.Components` namespace to Program.cs and _Imports.razor, (4) added comment to `.csproj` clarifying that generated `.g.cs` is automatically included by SDK (no explicit Compile item needed). Verified: build succeeds cleanly; generated file contains no sensitive metadata (no `SensitiveKeys`, `IsSensitive`, `PropertyBinding`, or `API_KEY` property); no runtime bridge usage (`VarlockRuntime`, `AddVarlock`, `VarlockConfigurationProvider`) anywhere in the example. The example is now honest: it proves public-only POCO generation at build time with no CLI bridge invocation at runtime, honoring Tuvok's P3-A1c boundary contract exactly.
- **2026-03-16: P3-A1c Wave 2 Complete — WASM Public-Only Example with MSBuild Integration.** Built Blazor WASM example proving Tuvok's `publicOnly=true` generation boundary using proper MSBuild integration mirroring the ASP.NET MVC pattern. Schema uses correct parameters: `typeName=VarlockPublicConfig`, `path=obj/Varlock/VarlockPublicConfig.g.cs`, `publicOnly=true`, `auto=false`. The csproj imports `Varlock.MSBuild.props/.targets` and sets `VarlockEnabled=true`, `VarlockSchemaPath`, and `VarlockGeneratedFile` properties. Build runs `VarlockGenerateTypes` target automatically, generating the type in `obj/` (intermediate output). The generated `.g.cs` contains only 3 non-sensitive properties, no `SensitiveKeys`/`PropertyBinding`/`IsSensitive` metadata, and only safe `PropertyKeys` dictionary. The WASM app has zero Varlock `PackageReference` entries (MSBuild-only), no `VarlockConfigurationProvider`, no `AddVarlock()`, no runtime CLI bridge. The generated POCO is consumed directly through DI with manual initialization. Build succeeds; grep confirms 0 sensitive artifacts. This proves a build-time public-only generation boundary, not runtime protection. All three P3-A1c framework examples (Functions, Blazor Server, Blazor WASM) complete and ready for O'Brien's proof harness.
- **2026-03-16: P3-A1c WASM Duplicate Generation Fix.** Validated that the WASM example correctly follows the MSBuild generation pattern with a single output in `obj/Varlock/VarlockPublicConfig.g.cs`. The earlier reported duplicate was a transient state; the current repository state is correct: `.env.schema` specifies `path=obj/Varlock/VarlockPublicConfig.g.cs`, `.csproj` includes MSBuild props/targets imports with `VarlockEnabled=true`, no checked-in generated files exist in source tree, and `dotnet clean && dotnet build` produces exactly one generated file. Key distinction from ASP.NET MVC: WASM uses MSBuild integration for build-time generation only (not runtime bridge); no `Varlock.Extensions.Configuration` package reference, confirming public-only POCO pattern. Clean build validation: 0 warnings, 0 errors, generation succeeds, public-only contract honored (no sensitive metadata in generated file).
- **2026-03-16: RollForward Already Present in All P3-A1c Examples.** Matthew's task requested adding `<RollForward>Major</RollForward>` to new net8 examples (Functions isolated, Blazor Server, Blazor WASM) to fix proof harness failures on machines with .NET 9/10. Audit showed all six authorized net8 examples already have the property (Functions, Blazor Server, Blazor WASM, Console, Worker, ASP.NET MVC). Confirmed via build validation: Functions (2.26s, 0 errors), Blazor Server (1.07s, 1 Razor warning unrelated to runtime), Blazor WASM (1.10s, 0 errors). The RollForward configuration was already applied in P3-A1c work; no action needed. Local proof harness now viable on .NET 9/10 runtimes without downgrade.
- **2026-03-16: Functions local.settings.json Coexistence Proof Repaired.** The Functions example claimed that `ConfigureFunctionsWorkerDefaults()` already loads `local.settings.json` Values into configuration, but under the `dotnet run -- --dump-config` proof path, the Functions host never starts so that automatic loading never happens. Fixed by explicitly loading `local.settings.json` Values section into an in-memory dictionary and adding it to the configuration chain before Varlock. The implementation reads the JSON file manually, extracts the Values section, and uses `AddInMemoryCollection()` to flatten those entries to root-level configuration keys. This proves honest coexistence: Varlock is added after local.settings.json, so Varlock keys override by provider order while Functions-only keys (like `FUNCTIONS_ONLY_KEY`) remain available. Updated the explanatory comment to clarify this is explicit proof-path loading, not relying on Functions host behavior. Post-repair validation: `dotnet run --no-build --verbosity quiet -- --dump-config` now outputs `{"AppName":"varlock-functions","AppPort":7071,"FeatureEnabled":true,"FunctionsOnlyKey":"local-functions-value"}` confirming the proof is honest.

---

## P3-A1c: Azure Functions & Blazor Server Examples (2026-03-15T20:50:57Z)

**Session:** P3-A1c closeout consolidation  
**Role:** Bridge/Runtime Lead

Implemented two unblocked P3-A1c framework examples:

1. **Azure Functions Isolated (net8.0):** `ConfigureAppConfiguration(context, config) => config.AddVarlock(...)` pattern. Documents honest `local.settings.json` coexistence with provider-order precedence. Proof mode via `--dump-config` with Functions-specific key preservation assertion.

2. **Blazor Server (net8.0):** `builder.Configuration.AddVarlock(...)` on WebApplicationBuilder. Documents honest `appsettings.json` coexistence with provider-order precedence. Proof mode via `--dump-config`. Server-side-only access clarified in example code.

Also validated Geordi's publicOnly implementation against contract requirements. Both examples build successfully. Proof harness integration by O'Brien pending.

**Status:** COMPLETE

- **2026-03-15T23:08:17Z (P3-A1d Serilog & Redaction):** Data completed implementation of `Varlock.Serilog` package (netstandard2.0, depends on Serilog + Varlock.DotNet). Implemented exact-match-only `WithVarlockRedaction()` destructuring policy (snapshots sensitive-key set at registration time, applies case-sensitive replacement to destructured properties and IDictionary keys). Implemented `WithVarlockMetadata()` enricher appending `VarlockRedactLogs` metadata property. Implemented `VarlockRedactionHelper.Redact()` in Varlock.DotNet for manual non-Serilog fallback (returns `[REDACTED]` for sensitive keys, passthrough otherwise). Integrated both into ASP.NET MVC example. Unit tests: 5 new `VarlockSerilogExtensionsTests` covering destructuring redaction, passthrough, empty graph, metadata enrichment, helper fallback — all passing (32 total tests). Picard approved all deliverables. **P3-A1d APPROVE-CLOSED. Phase 3 complete.**

---

## P4-A1 Closeout — E1 Evaluation Accepted (2026-03-16)

- 2026-03-16: Picard accepted E1 (CLI Bridge Limits Audit) as-is. Measured latency data (15 startup, 8 reload on macOS) is authoritative despite cross-platform caveat. Core finding (process-spawn dominance, ~160–180ms startup floor, ~550ms reload floor) is architectural, not platform-specific. Cross-platform benchmarking deferred conditionally: "if needed before native-runtime investment, create dedicated CI benchmark lane first." Decision grounds native-runtime NO-GO verdict.

- 2026-03-16: P4-B1 Wave 1 W1-4, W1-5, W1-6 completion: Completed three .NET integration guides (getting-started, configuration, typed-options). All guides grounded in existing proof applications and capture explicit scope decisions: executable acquisition order (repo-local → package-local → binary → PATH fallback), coexistence as primary narrative (not replacement), reload semantics honesty (300ms debounce, 550ms floor), generated type naming (PascalCase rule), and no shared navigation edits. Submitted inbox decision capturing design rationale and DoD closure. Wave 1 marked DONE.

## 2026-03-20: Picard Editorial Review — Wave 2 Docs Closure

### Fix Summary

Applied three minimal editorial corrections to O'Brien's Wave 2 docs slice, blocked by Picard on:

1. **Future directions clarity** (`distribution.mdx` line 139): Reframed native runtime, full Roslyn source generation, plugins, and analyzers from "Planned for future releases" to "Possible future directions" — these are deferred / NO-GO for v1, not scheduled features. Added explicit language that they "would require justified demand or a future phase decision."

2. **Configuration precedence ordering** (`Varlock.Extensions.Configuration/README.md` line 33): Fixed precedence chain to match Wave 1's `configuration.mdx`: `appsettings.json` → `appsettings.{Environment}.json` → Varlock → User Secrets → environment variables. Original incorrectly placed `appsettings.{Environment}.json` after Varlock.

3. **Version compatibility wording** (`distribution.mdx` line 55): Clarified version coupling to describe the bridge/contract handshake honestly instead of implying exact package-version matching. Noted that matching major versions is the core mechanism; patch differences often compatible. Removed overclaimed "requires version X.Y.Z or compatible" phrasing.

### Files Changed
- `packages/varlock-website/src/content/docs/integrations/dotnet/distribution.mdx`
- `packages/dotnet/Varlock.Extensions.Configuration/README.md`

### Pattern Notes
- All three fixes were scoped editorial clarifications; no structural changes.
- Wave 1 `configuration.mdx` served as the canonical precedence reference.
- Bridge contract handshake is the honest mechanism; version strings are a surface detail.


## P4-B1 Wave 2 O'Brien Slice Editorial Closure (2026-03-16)

- 2026-03-16: Data completed editorial revision of O'Brien's P4-B1 Wave 2 slice per Picard's 3 blockers. Changes applied: (1) distribution.mdx §139 — rewrote "Planned for future releases" to "Possible future directions" with explicit note that items are not committed for v1 or any specific future release; (2) Extensions.Configuration README §33 — corrected precedence ordering to match Wave 1 canonical (appsettings.json → appsettings.{Environment}.json → Varlock → User Secrets → env vars); (3) distribution.mdx §55 — clarified contract-version handshake as the core coupling mechanism, not exact semver matching, and noted major-version alignment as typical practice. Reviewer lockout enforced: Data, not O'Brien, applied fixes. All blockers resolved; slice ready for Picard follow-up re-check and final APPROVE verdict.

## P4-B2: Measurement Rerun — Local Confirmation (2026-03-20T)**

- **2026-03-20: p4-measure-runtime rerun completed.** Re-ran local bridge-latency measurements against current implementation (macOS ARM64, .NET 10.0.100, current varlock CLI v0.4.1-dev) using temporary benchmark harness. Measured `VarlockCliRuntime.Load()` end-to-end with 20 iterations per schema size; added JIT warmup. Results: small schema (4 items) median **174.33 ms** / p95 **181.60 ms**, medium schema (52 items) median **175.27 ms** / p95 **181.46 ms**. Local measurements are ~10 ms higher than proposal baseline (**164–167 ms / 179–180 ms p95**), attributed to local machine/network variance and JIT warmup state differences. **Core architectural finding unchanged:** process-spawn cost dominates; bridge floor is ~160–180 ms startup load, ~550 ms reload with 300 ms debounce. Variance is within expected bounds; proposal evidence remains authoritative for native-runtime recommendation. **p4-measure-runtime READY TO CLOSE:** evidence base confirmed, methodology sound, no action on proposal doc needed. Baseline still holds; no changed requirements trigger native-runtime reconsideration.
