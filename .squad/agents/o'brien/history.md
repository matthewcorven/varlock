# O'Brien — History

## Core Context

- **Project:** A first-class Varlock .NET support initiative built around a v1 CLI bridge, proof artifacts and support-matrix validation, and an explicit path to future native runtime or analyzer evolution.
- **Role:** Platform/Release Lead
- **Joined:** 2026-03-13T10:56:25.546Z

## Learnings

<!-- Append learnings below -->
- The first .NET implementation slice must ship with executable-distribution proof scaffolding, machine-readable contract-fixture scaffolding, and the first proving examples for console direct usage and ASP.NET provider usage; wider-platform, reload, and legacy proof can follow in the next slice.
- 2026-03-13: Picard and Data converged on a startup-only first slice under `packages/dotnet/` with CLI contract hardening in `packages/varlock`, so release and CI scaffolding should be sized to that narrower bridge boundary rather than hosted or platform-matrix claims.
- 2026-03-13: Tuvok's contract fixes bridge transport around stdout JSON envelopes and leaves `executable-not-found` to the caller, so acquisition and CI proof should validate both launch-time discovery failures and post-launch contract failures as separate cases.
- 2026-03-13: The first mergeable proof follow-up slice works best as runnable examples under `examples/` plus a single `bun run proof:dotnet` command, because that proves real `dotnet run` acquisition and startup behavior without pretending the wider platform matrix, options binding, or reload story is finished.
- 2026-03-13: The canonical proof boundary now fixes the first proven slice to the console and ASP.NET startup examples, with `test.yaml` running `bun run proof:dotnet` on Ubuntu and executable discovery limited to an explicit repo-relative `packages/varlock/bin/cli.js` path.
- 2026-03-13: Once the runtime grows node_modules/.bin and PATH fallback plus a real lang=cs specimen, the honest documentation move is to mark acquisition hardening as in progress, add a planned lookup-proof row, and keep C# generation planned until an example app actually consumes generated output or binder validation exists.
- 2026-03-13: Data fixed P1-A1 around deterministic lookup order plus a preflight bridge-contract mismatch probe, and Geordi fixed P1-B1 around dependency-free `PropertyBindings` metadata, so proof accounting should treat both as real implementation progress while keeping repository claims narrower than code state.
- 2026-03-13: When P1-A1 lookup hardening lands before a checked-in specimen, the docs should name the exact fallback order that exists in code, but the support row must stay planned until examples or CI run without an explicit ExecutablePath.
- 2026-03-13: With the checked-in examples now running without `ExecutablePath`, the ledger can mark repo-local development lookup as proven while keeping package-local, `node_modules/.bin`, and opt-in `PATH` fallback as planned until separate proof artifacts exercise them.
- 2026-03-13: This proof pass confirms the narrow threshold split for the new phase-1 lanes: P1-A1 now has repository-backed proof only for built-in repo-local lookup via `bun run proof:dotnet`, while P1-B1 remains implementation-only until a generated C# specimen is consumed by a compiled example or binder validation.
- 2026-03-13: After the package-local proof harness decision, the proven P1-A1 lookup surface is now exactly repo-local plus package-local acquisition; local `node_modules/.bin` and opt-in `PATH` fallback still require dedicated proof before the support matrix can widen.
- 2026-03-13: O'Brien re-verified P1-A1 after Data's lookup change by running `bun run proof:dotnet`; the checked-in proof harness now justifies marking package-local `node_modules/varlock/bin/cli.js` lookup as proven, while `node_modules/.bin` and opt-in `PATH` remain explicitly unproven and should stay out of widened support claims.
- 2026-03-13: My earlier proof-accounting note was overtaken by the checked-in repo state; after validation with `dotnet test` and `bun run proof:dotnet`, the honest P1-A1 proven surface is repo-local, package-local, and local `node_modules/.bin/varlock`, with only opt-in `PATH` fallback still unproven.
- 2026-03-13: Re-checking after Data's latest landing confirmed the repo still has no checked-in specimen or CI lane that executes opt-in `PATH` lookup; runtime tests cover lookup order in code, but proposal and ledger proof should stay limited to repo-local, package-local, and local `.bin` artifacts exercised by `bun run proof:dotnet`.
- 2026-03-13: That last proof-accounting objection was itself overtaken once the checked-in PATH harness and env-gated console seam landed; with fresh runtime-test and `bun run proof:dotnet` success, P1-A1 is now honestly complete and the stale inbox note should be cleared instead of merged back into the ledger.
- 2026-03-13: The narrowest honest P1-A2 move with today's checked-in startup/runtime packages is an explicit `dotnet build` proof for the console and ASP.NET examples inside `bun run proof:dotnet`; that widens the matrix by one build-flow row without claiming watch-mode, IntelliSense, or generated-file behavior.
- 2026-03-14: The checked-in repo-level MCP config already carried `context-mode`; the real drift risk was stale example JSON in shared templates and package READMEs, so proof here means keeping guidance synchronized instead of touching unrelated live server entries.
- 2026-03-14: Changed `context-mode` MCP server from PATH-based direct invocation to `npx -y context-mode` for consistency with all other MCP servers, enabling package-based distribution and future version pinning; updated both live `.copilot/mcp-config.json` and all example configs in templates and package docs while preserving uncommitted edits.
- 2026-03-15: Proof audit for P1-B1 autonomous completion via Ralph: verified P1-B1 proof slice materially complete with `bun run proof:dotnet` generating ASP.NET example's C# specimen, validating deterministic namespace/type-name overrides, and exercising binder-based projection through generated metadata and configuration-binder flow.
- 2026-03-15: P2-B1 proof-lane scoping in advance of Geordi's MSBuild package implementation; shadowing from proof/docs/CI side without blocking core work. Key insight: P2-B1 deepens the already-proven "explicit `dotnet build` example flow" row by proving **generated-file integration** within the MSBuild pipeline. Proof obligations: assert generated files land in `obj/Varlock/` after build, validate generated C# structure, check incremental caching behavior, prove optional validation surfaces as MSBuild diagnostics, and keep watch-mode/IntelliSense/complex binder validation explicitly deferred. MSBuild package owns property definitions and incremental wiring; proof owns assertions and ledger synchronization.



